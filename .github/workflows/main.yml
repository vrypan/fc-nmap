name: Farcaster-friendly Repo Page
  # You may pin to the exact commit or the version.
  # uses: vrypan/gh-frame@f02762c49e182b91a32fe6affa168770920b7d5d
  uses: vrypan/gh-frame@v2.0.0-pre14
  with:
    # GitHub token for authentication
    token: # optional, default is ${{ github.token }}
    # Custom domain for GitHub Pages
    cname: # optional
    # Branch name for GitHub Pages
    branch_name: # optional, default is gh-frame
    # CSS style use to render the page: light/dark/custom
    style: # optional, default is light
          
